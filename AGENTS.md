# Expo version contract

This app currently uses Expo SDK 57 (`expo ^57.0.8`). Before writing code,
read the exact versioned documentation at https://docs.expo.dev/versions/v57.0.0/.
Do not use APIs from another SDK or upgrade Expo as part of unrelated work. Treat an
SDK upgrade as its own reviewed, tested change.
