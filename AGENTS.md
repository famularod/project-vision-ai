# Expo version contract

This app currently uses Expo SDK 54 (`expo ~54.0.36`). Before writing code,
read the exact versioned documentation at https://docs.expo.dev/versions/v54.0.0/.
Do not use SDK 55/56 APIs or upgrade Expo as part of unrelated work. Treat an
SDK upgrade as its own reviewed, tested change.
