# FEATURE_SCORECARD — Vitruvius (2026-09-17)

Score definitions (from the guide): 0 absent, 1 stub/broken, 2 materially unreliable, 3 partial verification with known limitations, 4 passes defined representative acceptance, 5 broad repeatable acceptance with recovery/monitoring evidence.

Rule applied: this audit was static review plus offline unit/integration tests. No device, cloud or live-data acceptance was run, so **no area can score above 3 here**. A 3 means "the design and code support the promise and tests exercise real logic, but representative acceptance is not evidenced". Owner-reported device passes (Field Notes, individual canopy questions) are noted as history, not as current-build evidence.

Finding IDs refer to FINDINGS.md.

| # | Product area | Status | Quality | Evidence for the score | Blockers to reach 4 |
|---|---|---|---|---|---|
| 1 | Startup, identity, accounts | Partial | **2** | Token storage, owner sandbox mechanics, remount-per-owner and logging hygiene are solid (real-logic tests). But sign-out/owner switch deletes unsynced photos (ID-01), signed-out work is destroyed at sign-in (ID-02), offline start may be blocked after token expiry (ID-03). | ID-01, ID-02, ID-03; a cold-start-offline device test; a sign-out/sign-in photo survival test |
| 2 | Projects and areas | Implemented | **2** | Works through the offline queue. Create is not idempotent and projects are addressed by name (SYNC-03); identity slug collides and breaks on rename (ARCH-05); customer-specific defaults seeded (ARCH-06); GPS unit bug (GPS-01). Area GPS is suggestion-only, a good safety net. | SYNC-03, ARCH-05, GPS-01, ARCH-06 |
| 3 | Tasks and schedules | Implemented | **3** (single project) / **2** (multi-project) | Task create/edit/complete/delete with tombstones, timezone-aware list, CSV import, schedule engine, reconciliation: many unmocked tests. UTC "today" in lookahead (SCH-01); one "current" schedule across all projects drops the other projects' tasks (SCH-02); blind whole-row upsert across devices (SYNC-05). Reminders: **missing (0)**. Dependencies editable on web only. | SCH-02, SCH-01, SYNC-05; decide reminders scope |
| 4 | Field Notes and daily work | Implemented | **3** capture/offline/reopen; **2** conflict handling | Local-first verified writes, revision CAS, realtime, owner isolation tests. A note edited before first ack sticks forever in conflict with no UI (SYNC-01); possible timestamp-format false conflict (SYNC-02). Field updates (daily work): durable save with idempotency key, **3**. Owner previously reported device passes for Field Notes. | SYNC-01, confirm/kill SYNC-02 with one live row |
| 5 | Photos and field updates | Partial | **2** | Upload/retry/resume and the vision edge function are careful (**3** each). Library imports get "now" + current GPS, reversing AI before/after (PHO-02); unsynced photo deletion (ID-01, PHO-07); cover photo overwrites `project_data` (PHO-06). Photo-analysis answer quality: **not assessed**. | ID-01, PHO-02, PHO-06 |
| 6 | Documents and drawings | Partial | **2** | App-side intake, classification, readiness and revision activation are coherent (**3**). The indexer is all-or-nothing per document: a "verified" sheet number fails the PDF (IDX-01), unresolvable regions burn retries (IDX-02), count labels now block pages (IDX-03), sheet identity fitted to two projects (IDX-04), terminal failures shown as "will resume" (IDX-06). Exact page navigation on mobile: **missing** (share sheet only). Proof verification itself is strong. | IDX-01 to IDX-06; measure with appendix queries 5 to 7 |
| 7a | Ask ECOS (text/drawing Q&A) | Partial | **2** (drawing questions: **1**) | Scoping, spend control, proof binding and refusal wording are good. Real drawing questions: 0 of 10 on r4; `advanced-01` failed on r8 and r11; no real request has reached claim review (ECO-01). Deterministic override, silent passage drops, region-less text unusable, dead refusal path, hard-coded project logic, lexical "assurance", follow-up boilerplate, sheet-name failure (ECO-02 to ECO-09), latency (SRC-01/02, ECO-13). | ECO-01 closing evidence (ten questions with rubric, three devices) |
| 7b | Voice transcription | Implemented | **3** | Retry, sanitised errors, type-instead, recording kept on failure. No transcript review before sending (ECO-16); English only. | ECO-16 |
| 7c | Photo analysis | Implemented, owner-only | **2 to 3**, quality not assessed | Security and idempotency good; correctness of comparisons depends on PHO-02. | PHO-02; a labelled photo-pair set |
| 7d | Alerts / report intelligence | Implemented as local rules | **2** | Deterministic regex/threshold engines; much output computed and synced but never rendered; can present stale conclusions without an "as of" (PIE-07). | PIE-06, PIE-07 |
| 8 | Reports and exports | Partial | **2** | Deterministic text, fail-closed scoping, honest mail/SMS outcomes. Approval and all sharing blocked by any review flag with no acknowledge path (REP-01) and by `conflict_blocked` from ordinary task names (PIE-01); offline loses authority (PIE-03, PIE-04); snapshot saved at approve not send (REP-08); Word export embeds original photos (REP-09). PDF export: **missing**. | REP-01, PIE-01 (do the 10-minute device check first), PIE-03/04 |
| 9 | Offline and cross-device sync | Implemented | **3** mobile queue; **2** web<->mobile | The mobile queue is the strongest part of the codebase (journaled, single-flight, quarantine, tombstone gate, about 60 real-logic tests). Tombstones and revisions are enforced only in client code: hidden rows possible (WEB-01, WEB-02, SYNC-05). No background sync, no NetInfo. | DB trigger rejecting tombstoned ids + CAS RPC; WEB-01/02 |
| 10 | Backup, restore, DR | Partial | **2** | Crypto and restore transaction are sound (**3**, real-crypto tests). Scope is not: project records omitted (BAK-01), 128 MB all-or-nothing in memory (BAK-02), no cloud/scheduled backup, orchestration untested. Unversioned sole copy of deployed indexer source (SRC-04) is also a DR gap. | BAK-01, BAK-02, SRC-04; a wiped-device restore drill |
| 11 | Security and privacy (owner-only beta) | Implemented | **3** | RLS on all 57 tables, definer functions pinned, identity re-verified before service-role reads, fail-closed CORS, tight Deno/container permissions, no committed secrets. Capped by unverifiable deployed state (SEC-01, SEC-06), shared static worker token (SEC-05), one unmetered paid function (SEC-04), signup-dependent policies (SEC-02), data left on device (ID-06, PRIV-01). | Run appendix checks 1 to 4 + dashboard items; SEC-04, SEC-05 |
| 11b | Customer / team readiness | Missing | **1** | Singleton owner asserted in code, org-wide search without project scope (SEC-03), hard-coded project data and endpoints, no staging, no per-tenant storage policy. Tables with organisation fields do not make it multi-tenant. | Out of scope for owner beta; see roadmap phase 4 |
| 12 | Performance, cost, reliability | Partial | **2** | Virtualised lists, bounded provider spend in SQL, downsampled OCR. One 9,300-line component with 73 state hooks (PERF-16); whole-collection stringify diffs (PERF-17); PIE recompute + cloud writes per typing pause (PIE-06); about 29 sequential round trips per protected page open (SRC-01); memory peaks on proof open (WEB-10). Not measured on device. | PIE-06, PERF-17, SRC-01; a Hermes profile on an older iPad |
| 13 | Usability and accessibility | Partial | **2** | Honest state messaging and careful confirmations. 56% of `App.tsx` touchables unlabelled, shared buttons lack roles (UX-08); contrast tokens fail AA (UX-09); web focus/keyboard thin (UX-07); drafts lost silently on web (WEB-06); no web error boundary (WEB-05). | UX-08 (two components), UX-09 (tokens), WEB-05 |
| 14 | Architecture and maintainability | Partial | **2** | Zero import cycles, 388/399 files reachable, ratchets exist. 21k-line shell, 5k-line services, three Ask ECOS implementations under one slug, three diverged `_shared` trees, two migration trees, customer constants in shipped code. | REL-01, one source of truth per function, ARCH-06 |
| 15 | Testing and releases | Partial | **2** | Jest hygiene is good (264 suites / 1,897 tests, no skips; reproduced here). The gate is honest and fail-closed. But evidence binds to the wrong Ask ECOS code (REL-01), the app's Ask function is never executed by a test (REL-02), most script "tests" assert source text (REL-03), no handler-level Ask test (REL-04), CI cannot pass its last step (REL-05), no SHA in artifacts (REL-06), no staging (REL-07). | REL-01, REL-04, REL-05 |
| — | Desktop/web shell | Implemented | **3** | Solid auth, tab-scoped tokens, freshness design, guarded writes. It is a write client named "read-only" (WEB-03). | WEB-01, WEB-02, WEB-05 |
| — | Protected source service | Implemented | **3** correctness/security; **2** operability | Strong verification and container discipline; latency by design, timeout and concurrency mismatches, opaque errors, no CI (SRC-01 to SRC-08). | SRC-01 to SRC-03 |

## Not assessed (explicitly)

Answer correctness of any AI feature on real data; any behaviour on a physical device; Android; deployed database policy state; cloud routing and revisions; email/SMS delivery; App Store review readiness beyond the static plist check; Python worker tests (dependencies not installed in the audit environment).
